/**
 * DestinationStep — removed from the session flow.
 *
 * The Drive destination is now a base folder configured once in
 * Settings → Integrations → Drive base folder. Session creation
 * was moved to CustomerStep. This file is kept as a stub.
 */
export function DestinationStep(_props: { sessionKey: string }) {
  return null;
}
